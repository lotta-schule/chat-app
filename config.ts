export const apiBaseUrl = process.env.EXPO_PUBLIC_API_BASE_URL;
export const apiUrl = `${apiBaseUrl}/api`;
export const apiGqlUrl = `${apiUrl}/graphql`;

export const baseUrl = process.env.EXPO_PUBLIC_BASE_URL;
export const baseProtocol = process.env.EXPO_PUBLIC_BASE_PROTOCOL || 'https';
